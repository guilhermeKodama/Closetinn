const styles = theme => ({
  root: {
    color: 'white',
  },
  textField: {
    marginBottom: theme.spacing.unit,
  },
  button: {
    marginBottom: theme.spacing.unit,
  },
})

export default styles
